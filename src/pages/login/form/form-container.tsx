import styled from "@emotion/styled"

const FormContainer = styled.div`
    .title {
        color: var(--gray-10);
        font-size: 24px;
    }
    .desc {
        color: var(--gray-8);
        font-size: 16px;
        margin: 4px 0 20px;
    }
`

export default FormContainer
